import { GteValidatorDirective } from './gte-validator.directive';

describe('GteValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new GteValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
